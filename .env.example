# Stripe — obtén tus claves en https://dashboard.stripe.com/apikeys
# (activa el toggle "Test mode" arriba a la derecha para las claves de prueba)

# Clave pública, segura para exponer en el navegador
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Clave secreta, NUNCA la subas a git ni la expongas en el cliente
STRIPE_SECRET_KEY=sk_test_...
