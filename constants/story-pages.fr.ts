import type { StoryPageData } from "@/components/StoryPage"

/**
 * Contenu narratif (fr) des pages d'expédition. Les slugs présents ici sont
 * rendus avec StoryPage en français ; les autres retombent sur le modèle
 * compact ExpeditionDetail.
 */
export const storyPagesFr: Record<string, StoryPageData> = {}
