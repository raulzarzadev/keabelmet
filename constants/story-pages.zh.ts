import type { StoryPageData } from "@/components/StoryPage"

/**
 * 探险页面的叙事内容（中文）。此处存在的 slug 将以中文渲染 StoryPage；
 * 缺失的则回退到精简的 ExpeditionDetail 模板。
 */
export const storyPagesZh: Record<string, StoryPageData> = {}
